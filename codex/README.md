# Codex Skills

Codex skills live in `codex/skills/<skill-name>/` and must include `SKILL.md`.

To install a skill locally, copy it into `$CODEX_HOME/skills` (defaults to `~/.codex/skills`) and restart Codex.

